function concatenateData(p1, p2, p3, p4){
    let firstName = p1;
    let lastName = p2;
    let age = p3;
    let town = p4;

    //console.log("You are " + firstName + " " + lastName + " , a " + age + " years old from " + town);
    console.log(`You are ${firstName} ${lastName}, a ${age} years old from ${town}`)
}

concatenateData("Anton", "Ahmedov", "21", "Pazardzhik")